apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv
spec:
  storageClassName: pv-class
  capacity:
    storage: 100GB
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: default
    name: pvc
  gcePersistentDisk:
    pdName: pv-disk
    fsType: ext4
